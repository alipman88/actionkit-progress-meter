Copy and paste this HTML to embed a progress meter image:
<pre>
&lt;img src='<%= "#{request.host_with_port}/#{@sanitized_page_id}/#{@valid_hash}/#{@goal_type}/#{@goal}/baseball_bat.png" %>' /&gt;
</pre>